<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pits.athletestraining">

    <application
        android:name=".utils.AppGlobals"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.TeamsListActivity"
            android:label="@string/teams" />
        <activity android:name=".activities.TeamInfoActivity" />
        <activity android:name=".activities.TabsActivity" />
        <activity
            android:name=".activities.PlayerInfoActivity"
            android:label="@string/personal_data" />
        <activity android:name=".activities.TrainingSessionList" />
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".activities.TrainingSession"></activity>
    </application>

</manifest>